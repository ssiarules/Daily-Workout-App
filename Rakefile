require_relative './config/environment'

require 'sinatra/activerecord/rake' #Only want this line to load into memory when I want a rake task 

#will start pry

desc 'A console'
task :console do
    Pry.start
end 